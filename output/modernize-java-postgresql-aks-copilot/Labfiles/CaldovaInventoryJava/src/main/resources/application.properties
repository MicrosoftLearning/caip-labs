spring.application.name=caldova-inventory
server.port=8080

# Legacy deployment assumption: a local database credential is stored in source control.
spring.datasource.url=jdbc:postgresql://localhost:5433/caldova_inventory
spring.datasource.username=caldova
spring.datasource.password=Caldova_Local_2026!

management.endpoints.web.exposure.include=health,info
management.endpoint.health.probes.enabled=true
management.endpoint.health.show-details=never
